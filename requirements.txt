# SECURITY/HYGIENE FIXES (fresh-pass audit, Aug 2026):
#
# L-3 -- removed packages with zero import sites anywhere in app/, tests/, or
# scripts/ (confirmed via repo-wide grep before removal): motor, pymongo,
# httpx, loguru, PyJWT, langgraph, passlib, pydantic-settings.
# Unnecessary supply-chain surface for no benefit -- nothing in this codebase
# imports any of them.
# python-multipart is required again for /extract/tax-1040-carryforward (File/Form).
#
# L-5 -- every entry now has an upper bound. Previously everything but bcrypt
# and pydantic used a bare ">=" with no ceiling, so a fresh install could
# silently land on a new major (or, for pre-1.0 packages, minor) release with
# no warning. Floors below are the exact versions this pass installed fresh
# and ran the full test suite against; ceilings block the next breaking-change
# boundary while still allowing routine patch/security updates through.
openai>=2.53.0,<3.0.0
fastapi>=0.141.1,<0.142.0
python-multipart>=0.0.20,<1.0.0
uvicorn>=0.52.1,<0.53.0
python-dotenv>=1.2.2,<2.0.0
email-validator>=2.3.0,<3.0.0
pytest>=9.1.1,<10.0.0
black>=26.5.1,<27.0.0
isort>=8.0.1,<9.0.0
flake8>=7.3.0,<8.0.0
bcrypt>=4.0.1,<5.0.0
boto3>=1.43.68,<2.0.0
stripe>=15.5.0,<16.0.0
slowapi>=0.1.10,<0.2.0
pyotp>=2.10.0,<3.0.0
qrcode[pil]>=8.2,<9.0.0
cryptography>=50.0.0,<51.0.0
pydantic[email]>=2.13.4,<3.0.0
moto>=5.2.2,<6.0.0
pypdf>=5.0.0,<7.0.0
